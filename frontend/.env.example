# URL del endpoint Azure (FastAPI) cuando esté desplegado.
# En dev local apunta al backend levantado con `uvicorn app.main:app --port 8080`.
# Producción
NEXT_PUBLIC_INTEL_API_URL=https://leiten-intel-scraper.azurewebsites.net

# Desarrollo local
# NEXT_PUBLIC_INTEL_API_URL=http://localhost:8080
